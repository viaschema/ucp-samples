{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://viaschema.com/schemas/pii/types/pii_credential.json",
  "title": "PIICredential",
  "description": "Credential containing an opaque PII token from a trusted provider.",
  "type": "object",
  "required": ["type", "token"],
  "additionalProperties": true,
  "properties": {
    "type": {
      "type": "string",
      "description": "Credential type (e.g., 'token')."
    },
    "token": {
      "type": "string",
      "description": "Opaque PII token issued by the provider. The agent never sees the underlying PII data."
    }
  }
}
