{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://viaschema.com/schemas/lending/types/pii_credential.json",
  "title": "PII Credential",
  "description": "Credential containing an opaque PII token issued by a trusted PII provider.",
  "type": "object",
  "required": ["type", "token"],
  "additionalProperties": true,
  "properties": {
    "type": {
      "type": "string",
      "description": "Credential type, e.g. 'token'."
    },
    "token": {
      "type": "string",
      "description": "Opaque PII token from the provider. The agent never sees the underlying PII data."
    }
  }
}
