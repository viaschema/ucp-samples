{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://viaschema.com/schemas/lending/types/lending_response.json",
  "title": "LendingResponse",
  "description": "Container for lending state on a checkout. Tracks the lending workflow from PII collection through offer comparison.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "loan_type": {
      "type": ["string", "null"],
      "enum": ["personal", "car", null],
      "description": "Selected loan type."
    },
    "handlers": {
      "type": "array",
      "items": { "$ref": "https://viaschema.com/schemas/pii/types/pii_handler.json" },
      "description": "Available PII handlers from the merchant profile."
    },
    "lending_handler": {
      "$ref": "lending_handler.json",
      "description": "The lending handler managing this flow."
    },
    "lenders": {
      "type": "array",
      "items": { "$ref": "lender.json" },
      "description": "Available lenders for the selected loan type."
    },
    "offers": {
      "type": "array",
      "items": { "$ref": "loan_offer.json" },
      "description": "Loan offers sorted by rate (ascending)."
    },
    "status": {
      "type": ["string", "null"],
      "enum": ["consent_needed", "pii_missing", "offers_received", null],
      "description": "Lending workflow status."
    },
    "required_pii_fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "PII field names required for the loan type."
    },
    "required_non_pii_fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Non-PII field names required for the loan type."
    },
    "missing_pii_fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "PII fields that still need to be collected."
    }
  }
}
